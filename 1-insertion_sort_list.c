#include "sort.h"

/**
 * insertion_sort_list - function that sorts a doubly linked lis
 * @list: doubly linked list
 * Return: no return
 * **/

void insertion_sort_list(listint_t **list)
{
	listint_t *ptr, *tmp;

	if (!list)
		return;

	ptr = *list;
}
